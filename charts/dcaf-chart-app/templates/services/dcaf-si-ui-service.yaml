apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.website2.ui.service.name }}
spec:
  selector: {{- include "si-ui-label" . | nindent 3 }}
  ports:
    - protocol: TCP
      name: http
      port: {{ .Values.website2.ui.service.port }}
      targetPort: {{ .Values.website2.ui.service.targetPort }}
